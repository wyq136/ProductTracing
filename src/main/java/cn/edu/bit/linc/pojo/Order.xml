<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.edu.bit.linc.dao.IOrder">
    <select id="getOrderByBarcodeID" parameterType="Integer" resultType="cn.edu.bit.linc.pojo.Order">
        select * from order where barcodeid = #{barcodeID}
    </select>
    
    <select id="getOrderByUserID" parameterType="Integer" resultType="cn.edu.bit.linc.pojo.Order">
        select * from order where userid = #{userID}
    </select>
    
</mapper>