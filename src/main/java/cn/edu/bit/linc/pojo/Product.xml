<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.edu.bit.linc.dao.IProduct">
    <select id="getProducts" resultType="cn.edu.bit.linc.pojo.Product">
        select * from product
    </select>
    
    <select id="getProductsLocal" resultType="cn.edu.bit.linc.pojo.Product">
        select * from product where shop='local'
    </select>
    
    <select id="getProductInfos" resultType="cn.edu.bit.linc.pojo.ProductInfo">
        SELECT productid, component.componentid, componentname FROM productdetail, component WHERE productdetail.componentid = component.componentid
    </select>
    
    <select id="getLocalProductInfos" resultType="cn.edu.bit.linc.pojo.ProductInfo">
    	SELECT product.id as product_id, component.id as component_id, catalog.id as catalog_id, component_name, catalog_name FROM product, catalog, component 
	WHERE product.catalog_id = catalog.id AND product.id = component.product_id AND product.shop = 'local'
    </select>
    
    <select id="getRemoteProductInfos" resultType="cn.edu.bit.linc.pojo.ProductInfo">
    	SELECT product.id as product_id, component.id as component_id, catalog.id as catalog_id, component_name, catalog_name FROM product, catalog, component 
	WHERE product.catalog_id = catalog.id AND product.id = component.product_id AND product.shop != 'local'
	</select>
    
    <select id="getProductByID" resultType="cn.edu.bit.linc.pojo.Product">
        SELECT * FROM product WHERE productid = #{productID}
    </select>
    
    <select id="getProductByMerchantID" resultType="cn.edu.bit.linc.pojo.Product">
        SELECT * FROM product WHERE merchantID = #{merchantID}
    </select>
    
    <select id="getProductById" parameterType="String" resultType="cn.edu.bit.linc.pojo.Product">
        select * from product where productid = #{productID}
    </select>
    
    <insert id="addProduct" useGeneratedKeys="true" keyProperty="productID" parameterType="cn.edu.bit.linc.pojo.Product">
        insert into product (merchantid, productsku, productname, description, picture) values (#{merchantID}, #{productSKU},#{productName} ,#{description}, #{picture})
    </insert>
    
    <insert id="addProductDetail">
        insert into productdetail (productid, componentid) values (#{0}, #{1})
    </insert>
    
</mapper>
