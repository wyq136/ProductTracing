<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.edu.bit.linc.pojo.User">
    <select id="selectUserByUsername" parameterType="String" resultType="cn.edu.bit.linc.pojo.User">
        select * from `user` where username = #{username}
    </select>
    
    <select id="selectUser" parameterType="String" resultType="cn.edu.bit.linc.pojo.User">
        select * from `user`
    </select>
</mapper>